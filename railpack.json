{
  "$schema": "https://schema.railpack.com",
  "steps": {
    "build": {
      "commands": [
        "DJANGO_SECRET_KEY=build-placeholder-not-used-at-runtime DJANGO_DEBUG=False python backend/manage.py collectstatic --noinput"
      ]
    }
  },
  "deploy": {
    "startCommand": "python backend/manage.py migrate --noinput && python -m gunicorn core.wsgi:application --chdir backend --bind 0.0.0.0:${PORT:-8080} --workers 2 --timeout 120"
  }
}
